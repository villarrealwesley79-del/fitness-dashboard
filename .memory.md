# Project Memory: fitness-dashboard
_Generated: 2026-02-19 08:00_

## Architecture Decisions
- Used SQLite for local storage and JSON files for data persistence.
- Implemented an API-first design with Flask to allow easy integration of frontend components via RESTful endpoints.
- Created a dedicated client module (`oura_client.py`) for handling interactions with the Oura API.

## Naming Conventions
- Environment variables are stored in `.env` and loaded at runtime using `os.environ`.
- File names use lowercase letters, underscores as separators (e.g., `data_loader.py`).

## Key Patterns
- Data flow: The application uses a modular approach where different modules handle specific tasks such as data loading (`data_loader`) and API interaction (`oura_client`).
- API design: RESTful endpoints are used throughout the application to provide uniform access patterns.
- State management: Persistent states are managed through JSON files and SQLite databases, with clear separation of concerns between temporary runtime variables and persistent storage.

## Gotchas & Warnings
- Ensure `.env` file exists in the root directory for configuration loading.  
- API keys (like OURA_API_TOKEN) should be kept secure and not committed to version control.

## Open TODOs
- Add comprehensive unit tests for backend functionality.
- Refactor body measurement handling into its own module.
- Improve documentation on how to deploy and run the application in different environments.

## Manual Notes
_(preserved for user edits)_
